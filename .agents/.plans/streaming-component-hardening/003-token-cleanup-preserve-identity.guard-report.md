# Guard report — 003 token-cleanup-preserve-identity

**Recommendation: PASS** — identity preserved at the cleanup layer _and_ proven to keep the DOM mounted end-to-end; every done criterion (incl. the amended Step 4 remount assertion) reproduced green.
**Reviewed at** `17b9a14` · 2026-07-07 18:42 · **Plan planned at** `256a49f` (re-stamped from `939f154` on the Step-4 amendment)
**Integrated** — commit `08ae901` (Step 4 remount test + README status; identity fix in `b5f57b4`) · PR [#344](https://github.com/humanspeak/svelte-markdown/pull/344) · via the `commit` / `pr` skills

## Done criteria

| Criterion                                                                                                                | Result | Evidence                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm check` exits 0                                                                                                     | met    | `COMPLETED 1007 FILES 0 ERRORS 4 WARNINGS` — 4 warnings all pre-existing, unrelated (Image.svelte, Parser.svelte, _UnsupportedHTML.svelte, issue-195/CustomList.svelte)                                               |
| `pnpm test:only` exits 0; identity tests pass                                                                            | met    | `Test Files 145 passed · Tests 935 passed` (was 934 + 1 new remount test)                                                                                                                                             |
| Cleaning list/table twice preserves item/cell object identity (asserted by test)                                         | met    | `token-cleanup.test.ts` "preserve unchanged list item identity across clean passes" / "…table body cell identity…" → `.toBe` green                                                                                    |
| **Remount assertion (Step 4)**: streaming append growing a later item leaves an earlier unchanged item's DOM node intact | met    | `SvelteMarkdown.issue-328.test.ts:591` "keeps unchanged list item DOM mounted while a later item grows during streaming" → `itemAfter).toBe(itemBefore)` + `toHaveAttribute('data-remount-probe','kept')`, 17/17 pass |
| `listItemIndex` correct on all list items after clean                                                                    | met    | `token-cleanup.test.ts` asserts `listItemIndex === 0 / 1` after clean                                                                                                                                                 |
| No files outside the in-scope list modified                                                                              | met    | `git diff --stat main...HEAD` source = `token-cleanup.ts`, `token-cleanup.test.ts`, `SvelteMarkdown.issue-328.test.ts` only; no `render-metadata.ts` / reuse-walk touch                                               |
| Batch `README.md` row for 003 updated                                                                                    | met    | row 003 → `DONE`                                                                                                                                                                                                      |

## Spirit

The plan's `Why this matters` is that unchanged streaming rows/items **keep their DOM** — not merely that fewer objects are allocated. That intent is now actually delivered _and proven_, which is the whole reason for the Step-4 amendment. The mechanism holds end to end: `cleanListItem`/`cleanTableCell` (`token-cleanup.ts`) return the original object when `listItemIndex` matches and tokens are element-wise reference-equal; that preserved identity flows to the Svelte `{#each}` key via `getStableNodeKey` (`render-metadata.ts:116`), so the DOM node survives. The Step-4 test is the honest proof, not a checkbox: it stamps `data-remount-probe` on a live `<li>`, grows a _later_ sibling, and asserts both the same element reference and the survival of that hand-set attribute — a value that cannot survive a destroy/recreate. Object-identity unit tests prove the precondition; this proves the effect. No gap between letter and intent remains.

## Scope & conduct

- **In-scope only?** Yes. Committed source: `token-cleanup.ts` + `token-cleanup.test.ts`. Uncommitted at review: the Step-4 test in `SvelteMarkdown.issue-328.test.ts` (a component-level `SvelteMarkdown.*.test.ts`, permitted by the amended In-scope) + the README status flip. The test reuses the pre-existing `TrackedListItem.svelte` helper — no new source component. Placing the test in the existing issue-328 streaming-stability suite (next to analogous "keeps a stable … mounted" tests) matches convention rather than fighting it.
- **STOP conditions respected?** Yes. The critical one for Step 4 — "do not modify `render-metadata.ts` or the reuse walk to make the remount test pass; a failing assertion is a STOP" — was honored: the test passes against the _unchanged_ render layer (`git diff` shows zero render-layer edits), meaning the cleanup-layer identity fix alone is sufficient. Drift check clean (`token-cleanup.ts` delta vs `256a49f` is exactly the executor's own fix; removed lines matched the plan excerpts at Checkpoint 1).
- **Plan amendments during execution:** one — 2026-07-07 (guard), with operator agreement: added Step 4 + a Done criterion + Test-plan entry for the end-to-end remount assertion, widened In-scope for one remount-test file, re-stamped `Planned at` `939f154` → `256a49f`. Rationale: the original criteria proved identity only at the cleanup layer, one altitude below the plan's stated DOM-preservation intent (plan defect, not executor drift; the bar was raised, not lowered).

## Residual risk / follow-ups

- **No tampering.** The executor updated only the README status row (which the plan instructs it to do); the plan `.md` and the guard log were left to guard.
- **Coupling to the reuse layer (plan 011).** This fix widens how often `reuseStableStreamingTokens` keeps a node by reference. When 011 lands and unifies the reuse identity rule, re-run the Step-4 remount test and the identity unit tests — they are the regression guard for this behavior. (Already noted in the plan's Maintenance notes.)
- **Header-cell identity** is exercised by code (`cleanTableCell` serves both header and rows) but only body cells have explicit `.toBe` assertions. Low risk; same code path.
