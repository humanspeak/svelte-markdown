# Guard report — 008 remove-dead-html-code

**Recommendation: PASS** — every Done criterion reproduced green from scratch, and the diff is a pure deletion: every added line in `src/lib` is a comment or a trimmed import.
**Reviewed at** `c974b80` · 2026-07-09 12:16 · **Plan planned at** `939f154`
**Integrated** — PR <https://github.com/humanspeak/svelte-markdown/pull/351> opened via the `pr` skill for the reviewed snapshot commit `c974b80` (guard log `1ce413b` rides along). Merging remains the operator's call.

## Done criteria

| Criterion                                                                             | Result  | Evidence                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `grep -rn "parseHtmlBlock\|processHtmlTokens\|containsMultipleTags" src` → no matches | **met** | Re-run at close-out → `no matches`. Also `TAG_REGEX` (their private constant) → none.                                                                                                                                                                          |
| `grep -rn "stream-benchmark" src` → no matches (or documented stop)                   | **met** | Re-run → `no matches`. Full removal; the documented-stop escape hatch was not needed.                                                                                                                                                                          |
| `pnpm check` exits 0                                                                  | **met** | `CHECK_EXIT=0`; `COMPLETED 1002 FILES 0 ERRORS 4 WARNINGS`. The 4 warnings are pre-existing `state_referenced_locally` in `_UnsupportedHTML`/`Image`/`Parser`/`CustomList`.                                                                                    |
| `pnpm test:only` exits 0                                                              | **met** | `TEST_EXIT=0`; **141 files, 901 tests passed**. (145/944 before — the delta is exactly the 4 deleted files.)                                                                                                                                                   |
| `pnpm build` exits 0                                                                  | **met** | `BUILD_EXIT=0`, publint `All good!`. Run after `rm -rf dist` so the audit below is a real clean build, not stale output.                                                                                                                                       |
| `trunk fmt && trunk check` exits 0                                                    | **met** | `FMT_EXIT=0`, `TRUNK_EXIT=0`, `✔ No issues` over 5 modified files. Tree stayed clean after `fmt` → no formatting drift.                                                                                                                                        |
| Only in-scope files modified; ≤1 file outside `src/lib`                               | **met** | `git diff --name-status f5f1e4c...HEAD` → 6 files under `src/lib/utils/`, the batch `README.md`, guard's own log, and **exactly one** outside `src/lib`: `src/routes/test/perf-bench/+page.svelte` — the file the plan names. `src/lib/index.ts` diff → empty. |
| Batch `README.md` status row for 008 updated                                          | **met** | Row 008 flipped `TODO` → `DONE`.                                                                                                                                                                                                                               |

**Beyond the checklist** — the plan's maintenance note asks a reviewer to confirm the built package no longer carries the bench module. Verified against a clean build: `ls dist/utils/ | grep stream-benchmark` → absent; `grep -rln "<legacy symbols>" dist/` → absent.

## Spirit

The plan's `Why this matters` is not "make three greps return nothing" — it is _stop shipping a second, divergent copy of the trickiest logic in the module._ That is genuinely delivered. `processHtmlTokens` was a full stack-based tag-pairing implementation running in parallel with the live `pairFlatHtmlTokens`; it is gone, along with the flat tokenizer and the duplicate multi-tag detector, and `token-cleanup.ts` drops 762 → 516 lines. The module now has exactly one HTML-pairing implementation, which is the stated end state.

The strongest evidence that no behavior changed is structural rather than circumstantial. I diffed the surviving code and every added non-comment line in `src/lib` is a single trimmed import in `token-cleanup.test.ts`; **not one line of the live path was rewritten.** The greps are satisfied by real deletion, not by a rename that slips past them, and the test file was trimmed of exactly the `describe('parseHtmlBlock')` block and one stale comment — the `isHtmlOpenTag` and `shrinkHtmlTokens` suites stand untouched. No criterion was gamed; nothing was weakened to make a check go green.

Two things I checked precisely because a plan-authorized deletion can still quietly cost something. First, orphaned helpers: `isHtmlOpenTag` (`token-cleanup.ts:311`), `serializeAttributes` (`:206,215`), and `SELF_CLOSING_TAGS` (`:58,203`) are all still used by the live path, and the only constant removed (`TAG_REGEX`) belonged solely to a deleted function — so STOP #3 never applied. Second, coverage: the deleted `stream-benchmark.test.ts` asserted incremental-parse ≡ full-parse for prose, late-closing code fences, and tables. That equivalence survives independently in `incremental-parser.test.ts` (streamed fences `:155`, tables `:167`, explicit `lexAndClean` equality `:290,:331`). The deletion cost no unique coverage.

## Scope & conduct

- **In-scope only?** Yes. One file outside `src/lib` — `src/routes/test/perf-bench/+page.svelte` — which the plan explicitly designates as the single allowed touch. `src/lib/index.ts` untouched, per the out-of-scope clause.
- **STOP conditions respected?** Yes, all three — and the first deserves explanation, because the plan's drift check **did not come back clean**. `git diff --stat 939f154..HEAD -- src/lib/utils/token-cleanup.ts` shows +101/−13 (plan 007 landed in that file after 008 was written), and the plan says "mismatch ⇒ STOP." So rather than wave it through, I re-verified each "Current state" fact at the executor's parent `f5f1e4c`: all three legacy symbols had only their own definitions plus test files as referencers, none appeared in `index.ts`, and `stream-benchmark` had exactly the two referencers the plan predicted. The drift moved the file but not the facts, and STOP #1 triggers only on a symbol _gaining_ a production importer. Proceeding was correct. STOP #2 (route rework) — see below. STOP #3 (shared helper removed) — never applied; nothing shared was deleted and the suite confirms it.
- **Deviation from Step 3, ratified by the operator.** Step 3 permits touching the dev route "**only** to drop the `stream-benchmark` import and whatever UI depended on it." The executor instead inlined a 22-line `benchmarkAppendParse` port (`+page.svelte:365-385`), preserving all three benchmark scenarios. Strictly more than the letter allows. Both paths the plan literally offered were worse against its own intent: deleting the UI would "gut the dev page," which Step 3 expressly forbids, and stopping would leave `stream-benchmark.ts` shipping — the very thing the plan exists to prevent. I verified the port rather than assuming it: it returns exactly the four fields the route consumes (`chunkCount`, `totalParseMs`, `p95ParseMs`, `peakParseMs` — used at `:732-734, :853-855, :933-935`), the dropped `totalChars`/`finalTokens`/`parseDurationsMs` had no consumer, and `p95` calls the route's pre-existing `percentile` (`:353-358`) whose body is logically identical to the deleted module's, so reported percentiles do not shift. Surfaced to the operator at checkpoint 1 and **explicitly approved** ("I am ok with the change") before this gate. Scope was not widened to launder it — the work passes on intent as written.
- **Plan amendments during execution:** none. The plan file `008-remove-dead-html-code.md` was never modified (`git diff f5f1e4c...HEAD -- .../008-*.md` → empty). The executor's only plan-directory edit was the batch `README.md` row, which the plan's executor-instructions block and Done criterion 5 both require. No tampering.

## Residual risk / follow-ups

- **Branch is 2 commits behind `origin/main`** at time of PR (`8ba3135` version bump, `ec9a495` `feat(streaming): streamId prop` #350). I checked for a resurrected dependency: #350 touches `SvelteMarkdown.svelte`, `types.ts`, and new stream-id tests, **none** of which reference the deleted symbols, and there is **zero file overlap** with this PR. No conflict and no reintroduced importer expected. Still worth letting CI on #351 confirm against the merged state before merging.
- **`stream-benchmark.ts` is gone as a reusable module.** If a future benchmark needs `benchmarkAppendStream` outside the `perf-bench` route, it must be re-extracted — deliberately, and into a dev-only location, not back into `src/lib`.
- **Maintenance note now in force:** `pairFlatHtmlTokens` is the _only_ HTML-pairing implementation. Future HTML-nesting fixes go there and nowhere else. The duplicate that made drift possible no longer exists.
- **Plan 009 lists 008 as a soft dependency** and is now unblocked once #351 merges.
- **Coverage thresholds are not yet enforced** (that is plan 010's job), so the 44-test reduction is not gated by CI. Accounted for above; noted so a merger is not surprised by the count.
